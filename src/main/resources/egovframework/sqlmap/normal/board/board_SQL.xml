<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://www.ibatis.com/dtd/sql-map-2.dtd">

<sqlMap namespace="board">

	<typeAlias  alias="egovMap" type="egovframework.rte.psl.dataaccess.util.EgovMap"/>
	<typeAlias  alias="searchVO" type="egovframework.normal.board.service.BoardDefaultVO"/>
	<typeAlias  alias="boardVO" type="egovframework.normal.board.service.BoardVO"/>
	
	<resultMap id="board" class="egovframework.normal.board.service.BoardVO" >
		<result property="boardSq" column="board_sq"/>
		<result property="boardCd" column="board_cd"/>
		<result property="title" column="title"/>
		<result property="content" column="content"/>
		<result property="viewCnt" column="view_cnt"/>
		<result property="userNm" column="user_nm"/>
		<result property="useYn" column="use_yn"/>
		<result property="createDt" column="create_dt"/>
		<result property="updateDt" column="update_dt"/>
	</resultMap>


	<insert id="boardDAO.insertBoard" parameterClass="egovframework.normal.board.service.BoardVO" >
		
		<![CDATA[
			INSERT INTO test_board_tb 
				( board_sq
				  , board_cd
				  , title
				  , content
				  , user_nm
				  )
			VALUES ( test_board_seq.nextval
				  , #boardCd#
				  , #title#  
				  , #content#
				  , #userNm#
					 )
		]]>
		<selectKey keyProperty="boardSq"  resultClass="Long">
			SELECT test_board_seq.currval AS board_sq FROM dual
		</selectKey>
	</insert>
	
	<update id="boardDAO.updateBoard">
		<![CDATA[
			UPDATE test_board_tb
			SET title=#title#
				, content=#content#
				, update_dt=systimestamp
				, user_nm = #userNm#
				, board_cd = #boardCd#
			WHERE board_sq=#boardSq#
		]]>
	</update>
	
	<update id="boardDAO.updateViewCnt">
		<![CDATA[
			UPDATE test_board_tb
			SET view_cnt = view_cnt + 1
			WHERE board_sq=#boardSq#
		]]>
	</update>
	
	
	<delete id="boardDAO.deleteBoard">
		<![CDATA[
			UPDATE test_board_tb
			SET use_yn = 'N'
			WHERE board_sq=#boardSq#
		]]>
	</delete>
    
    <select id="boardDAO.selectBoard" resultMap="board" remapResults="true">
       	<![CDATA[
            SELECT
            	board_sq, board_cd, title, content, view_cnt, user_nm, use_yn,
            	TO_CHAR(update_dt,'YYYY.MM.DD HH24:MI')update_dt, create_dt
            FROM test_board_tb 
            WHERE board_sq=#boardSq#
            	AND use_yn = 'Y'
        ]]>
    </select>
	
	<select id="boardDAO.selectBoardList"  parameterClass="searchVO" resultClass="egovMap">

			SELECT
				board_sq, board_cd, title, view_cnt, user_nm, TO_CHAR(update_dt,'YYYY.MM.DD')update_dt
			FROM test_board_tb
			WHERE 1=1 and use_yn = 'Y'
			<isEmpty prepend="AND" property="boardCd">
				board_cd = 'B0101'
			</isEmpty>
			<isNotEmpty prepend="AND" property="boardCd">			
				board_cd = #boardCd#
			</isNotEmpty>
			<isNotEmpty property="searchCondition">
				<isEqual prepend="AND" property="searchCondition" compareValue="0">
					title LIKE '%' || #searchKeyword# || '%'
				</isEqual>
				<isEqual prepend="AND" property="searchCondition" compareValue="1">
					content LIKE '%' || #searchKeyword# || '%'
				</isEqual>
				<isEqual prepend="AND" property="searchCondition" compareValue="2">
					user_nm LIKE '%' || #searchKeyword# || '%'
				</isEqual>
			</isNotEmpty>		
			
			
			ORDER BY board_sq DESC
			OFFSET #firstIndex# ROWS 
			FETCH NEXT #recordCountPerPage# ROWS ONLY	
	</select>
	

	
	<select id="boardDAO.selectBoardListTotCnt" parameterClass="searchVO" resultClass="int">

			SELECT COUNT(*) totcnt
			FROM test_board_tb
			WHERE 1=1 and use_yn = 'Y'
			<isEmpty prepend="AND" property="boardCd">
				board_cd = 'B0101'
			</isEmpty>
			<isNotEmpty prepend="AND" property="boardCd">			
				board_cd = #boardCd#
			</isNotEmpty>
			<isEqual prepend="AND" property="searchCondition" compareValue="0">
				title LIKE '%' || #searchKeyword# || '%'
			</isEqual>
			<isEqual prepend="AND" property="searchCondition" compareValue="1">
				content LIKE '%' || #searchKeyword# || '%'
			</isEqual>
			<isEqual prepend="AND" property="searchCondition" compareValue="2">
				user_nm LIKE '%' || #searchKeyword# || '%v'
			</isEqual>
	</select>
	
	<select id="boardDAO.boardPrevNext"  parameterClass="boardVO" resultClass="egovMap">
	<![CDATA[
 		SELECT DISTINCT t2.next_no, t2.prev_no 
 		  FROM (SELECT rownum, lag(rownum)over(ORDER BY rownum) AS prev_no, lead(rownum)over(ORDER BY rownum) AS next_no, board_sq 
				FROM (SELECT board_sq, board_cd, title, content, view_cnt, user_nm,  
 						 use_yn, to_char(update_dt, 'yyyy.mm.dd hh24:mi')as update_dt , create_dt  
 					FROM TEST_BOARD_TB  
 				   WHERE use_yn = 'Y' AND board_cd = #boardCd#)t1 
 				    				ORDER BY rownum)t2 
		 WHERE board_sq = #boardSq# 
 	]]> 
	</select>

	<select id="boardDAO.selectPrevNext" parameterClass="boardVO" resultClass="boardVO">
	<![CDATA[
 		SELECT t2.board_sq, t2.board_cd, t2.title, t2.content, t2.view_cnt, t2.user_nm,  
 		  		   t2.use_yn, t2.update_dt , t2.create_dt 	 
 		FROM (SELECT rownum, lag(rownum)over(ORDER BY rownum) AS prev_no, lead(rownum)over(ORDER BY rownum) AS next_no, t1.* 
 					FROM (SELECT board_sq, board_cd, title, content, view_cnt, user_nm,  
 							 use_yn, to_char(update_dt, 'yyyy.mm.dd hh24:mi')as update_dt , create_dt  
 						FROM TEST_BOARD_TB  
 					   WHERE use_yn = 'Y' AND board_cd = #boardCd#)t1 
 					ORDER BY rownum)t2 
 	  ]]> 
 	  
 	  <isParameterPresent prepend="WHERE">
	 	  <isNotNull property="prevNextCondition">
			   <isEqual property="prevNextCondition" compareValue="next">
			   		 next_no = #nextNo#
			   </isEqual>
	 	  </isNotNull>
	 	  
	 	  <isNotNull property="prevNextCondition">
			   <isEqual property="prevNextCondition" compareValue="prev">
			   		 prev_no = #prevNo#
			   </isEqual>
	 	  </isNotNull>
 	  </isParameterPresent>
 	  
	</select>  
	

</sqlMap>
